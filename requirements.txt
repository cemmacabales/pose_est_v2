# ── Runtime deps (RPi 5 + Mac) ──
ai-edge-litert
opencv-python
numpy
Pillow
gtts
playsound==1.2.2
qrcode[pil]
flask
python-dotenv
pytest

# ── RAG / LLM runtime deps ──
groq
onnxruntime
tokenizers

# ── Pose estimation (runtime) ──
# NOTE: mediapipe has NO official Linux ARM64 wheel.
# On RPi 5: rpi_setup.sh builds from source (see script for alternatives).
# On Mac: installs normally via pip.
mediapipe

# ── Build-time only (Mac/Colab — NOT RPi 5) ──
# Uncomment below on Mac/Colab for knowledge base building:
# pymupdf
# sentence-transformers
# torch
# transformers
#
# TensorFlow training (Mac only — use .venv-tf with Python 3.11):
#   pip install tensorflow
